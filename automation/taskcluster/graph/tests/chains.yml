---
- task:
    metadata:
      name: Chains tests
      description: Chains tests

    payload:
      maxRunTime: 7200
      env:
        NSS_TESTS: chains

    extra:
      treeherder:
        symbol: Chains
