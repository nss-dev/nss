---
- task:
    metadata:
      name: "EC tests"
      description: "EC tests"

    payload:
      command:
        - "/bin/bash"
        - "-c"
        - "bin/checkout.sh && nss/automation/taskcluster/scripts/run_tests.sh"
      env:
        NSS_TESTS: "ec"

    extra:
      treeherder:
        symbol: EC
