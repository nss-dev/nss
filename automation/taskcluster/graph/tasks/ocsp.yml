---
- task:
    metadata:
      name: "OCSP tests"
      description: "OCSP tests"

    payload:
      command:
        - "/bin/bash"
        - "-c"
        - "bin/checkout.sh && nss/automation/taskcluster/scripts/run_tests.sh"
      env:
        NSS_TESTS: "ocsp"

    extra:
      treeherder:
        symbol: OCSP
