---
- task:
    metadata:
      name: "DB tests"
      decription: "DB tests"

    payload:
      command:
        - "/bin/bash"
        - "-c"
        - "bin/checkout.sh && nss/automation/taskcluster/scripts/run_tests.sh"
      env:
        NSS_TESTS: "dbtests"

    extra:
      treeherder:
        symbol: DB
