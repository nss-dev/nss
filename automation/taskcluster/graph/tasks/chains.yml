---
- task:
    metadata:
      name: "Chains tests"
      description: "Chains tests"

    payload:
      command:
        - "/bin/bash"
        - "-c"
        - "bin/checkout.sh && nss/automation/taskcluster/scripts/run_tests.sh"
      env:
        NSS_TESTS: "chains"

    extra:
      treeherder:
        symbol: Chains
