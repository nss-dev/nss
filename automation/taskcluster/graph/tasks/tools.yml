---
- task:
    metadata:
      name: "Tools tests"
      decription: "Tools tests"

    payload:
      command:
        - "/bin/bash"
        - "-c"
        - "bin/checkout.sh && nss/automation/taskcluster/scripts/run_tests.sh"
      env:
        NSS_TESTS: "tools"

    extra:
      treeherder:
        symbol: Tools
